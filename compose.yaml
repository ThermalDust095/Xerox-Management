services:
  flask_app:
    build:
      context: .
    container_name: flask_app
    depends_on:
      mysql_db:
        condition: service_healthy
    ports:
      - ${PORT}:5000
    volumes:
      - .:/app
      - ./files:/app/files
    environment:
      - FLASK_ENV=${FLASK_ENV}
