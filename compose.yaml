services:
  flask_app:
    build:
      context: .
    container_name: flask_app
    # ports:
    #   - 5000:5000
    volumes:
      - .:/app
      - ./files:/app/files
    environment:
      - FLASK_ENV=${FLASK_ENV}
